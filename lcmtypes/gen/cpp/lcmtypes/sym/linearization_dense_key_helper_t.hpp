/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <lcm/lcm_coretypes.h>

#ifndef __sym_linearization_dense_key_helper_t_hpp__
#define __sym_linearization_dense_key_helper_t_hpp__

#include <vector>

namespace sym
{

/**
 * ------------------------------------------------------------------------------------------------
 * Helpers for building a full optimization problem from linearized factors
 * ------------------------------------------------------------------------------------------------
 * Index information for a single key within a linearized factor
 * Aids with rapid repeated linearization
 */
class linearization_dense_key_helper_t
{
    public:
        // Offset of this key within the factor's state vector
        int32_t factor_offset;

        // Offset of this key within the whole problem's state vector
        int32_t combined_offset;

        // Tangent dimension of the key
        int32_t tangent_dim;

        std::vector< int32_t > jacobian_storage_col_starts;

        // For each other key (from 0 to this key's index), the sparse storage valuePtr array offsets
        // NOTE(hayk): Currently num_other_cols is not constant in actual use - this type likely
        // can't be serialized right now. Perhaps try to store a flatter structure of slices.
        int32_t num_other_keys;

        int32_t num_other_cols;

        std::vector< std::vector< int32_t > > hessian_storage_col_starts;

    public:
        linearization_dense_key_helper_t() = default;

        /**
         * Member constructor
         */
        inline linearization_dense_key_helper_t(
            const int32_t& factor_offset_arg,
            const int32_t& combined_offset_arg,
            const int32_t& tangent_dim_arg,
            const std::vector< int32_t >& jacobian_storage_col_starts_arg,
            const int32_t& num_other_keys_arg,
            const int32_t& num_other_cols_arg,
            const std::vector< std::vector< int32_t > >& hessian_storage_col_starts_arg
        );

        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        constexpr static int64_t getHash();

        using type_name_array_t = const char[33];

        inline static constexpr type_name_array_t* getTypeNameArrayPtr();

        /**
         * Returns "linearization_dense_key_helper_t"
         */
        inline static constexpr const char* getTypeName();

        /**
         * Returns "sym"
         */
        inline static const char * getPackageName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        constexpr static uint64_t _computeHash(const __lcm_hash_ptr *p);

        // Comparison operators.
        inline bool operator==(const linearization_dense_key_helper_t& other) const;
        inline bool operator!=(const linearization_dense_key_helper_t& other) const;
};

linearization_dense_key_helper_t::linearization_dense_key_helper_t(
    const int32_t& factor_offset_arg,
    const int32_t& combined_offset_arg,
    const int32_t& tangent_dim_arg,
    const std::vector< int32_t >& jacobian_storage_col_starts_arg,
    const int32_t& num_other_keys_arg,
    const int32_t& num_other_cols_arg,
    const std::vector< std::vector< int32_t > >& hessian_storage_col_starts_arg
) : factor_offset(factor_offset_arg),
    combined_offset(combined_offset_arg),
    tangent_dim(tangent_dim_arg),
    jacobian_storage_col_starts(jacobian_storage_col_starts_arg),
    num_other_keys(num_other_keys_arg),
    num_other_cols(num_other_cols_arg),
    hessian_storage_col_starts(hessian_storage_col_starts_arg) {}

int linearization_dense_key_helper_t::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = (int64_t)getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int linearization_dense_key_helper_t::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int linearization_dense_key_helper_t::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

constexpr int64_t linearization_dense_key_helper_t::getHash()
{
    return _computeHash(NULL);
}

constexpr linearization_dense_key_helper_t::type_name_array_t* linearization_dense_key_helper_t::getTypeNameArrayPtr() {
    return &"linearization_dense_key_helper_t";
}

constexpr const char* linearization_dense_key_helper_t::getTypeName()
{
    return *linearization_dense_key_helper_t::getTypeNameArrayPtr();
}

const char * linearization_dense_key_helper_t::getPackageName()
{
    return "sym";
}

int linearization_dense_key_helper_t::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->factor_offset, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->combined_offset, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->tangent_dim, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    int32_t v_num_jacobian_storage_col_starts = this->jacobian_storage_col_starts.size();
    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &v_num_jacobian_storage_col_starts, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(v_num_jacobian_storage_col_starts > 0) {
        tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->jacobian_storage_col_starts[0], v_num_jacobian_storage_col_starts);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_other_keys, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_other_cols, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_other_cols > 0) {
        for (int a0 = 0; a0 < this->num_other_keys; a0++) {
            tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->hessian_storage_col_starts[a0][0], this->num_other_cols);
            if(tlen < 0) return tlen; else pos += tlen;
        }
    }

    return pos;
}

int linearization_dense_key_helper_t::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->factor_offset, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->combined_offset, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->tangent_dim, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    int32_t v_num_jacobian_storage_col_starts;
    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &v_num_jacobian_storage_col_starts, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    this->jacobian_storage_col_starts.resize(v_num_jacobian_storage_col_starts);
    if(v_num_jacobian_storage_col_starts) {
        tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->jacobian_storage_col_starts[0], v_num_jacobian_storage_col_starts);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_other_keys, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_other_cols, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    this->hessian_storage_col_starts.resize(this->num_other_keys);
    for (int a0 = 0; a0 < this->num_other_keys; a0++) {
        this->hessian_storage_col_starts[a0].resize(this->num_other_cols);
        if(this->num_other_cols) {
            tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->hessian_storage_col_starts[a0][0], this->num_other_cols);
            if(tlen < 0) return tlen; else pos += tlen;
        }
    }

    return pos;
}

int linearization_dense_key_helper_t::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, this->jacobian_storage_col_starts.size());
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += this->num_other_keys * __int32_t_encoded_array_size(NULL, this->num_other_cols);
    return enc_size;
}

constexpr uint64_t linearization_dense_key_helper_t::_computeHash(const __lcm_hash_ptr *)
{
    uint64_t hash = 0x381e5cdfec61c814LL;
    return (hash<<1) + ((hash>>63)&1);
}

bool linearization_dense_key_helper_t::operator==(const linearization_dense_key_helper_t& other) const {
  return (
          (factor_offset==other.factor_offset) && 
          (combined_offset==other.combined_offset) && 
          (tangent_dim==other.tangent_dim) && 
          (jacobian_storage_col_starts==other.jacobian_storage_col_starts) && 
          (num_other_keys==other.num_other_keys) && 
          (num_other_cols==other.num_other_cols) && 
          (hessian_storage_col_starts==other.hessian_storage_col_starts));
}

bool linearization_dense_key_helper_t::operator!=(const linearization_dense_key_helper_t& other) const {
  return !(*this==other);
}

}

#endif