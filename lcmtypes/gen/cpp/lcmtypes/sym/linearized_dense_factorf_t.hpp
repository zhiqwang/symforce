/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <lcm/lcm_coretypes.h>

#ifndef __sym_linearized_dense_factorf_t_hpp__
#define __sym_linearized_dense_factorf_t_hpp__

#include "lcmtypes/eigen_lcm/MatrixXf.hpp"
#include "lcmtypes/eigen_lcm/VectorXf.hpp"
#include "lcmtypes/sym/index_t.hpp"

namespace sym
{

/// Same as linearized_dense_factor_t but for floats.
class linearized_dense_factorf_t
{
    public:
        ::sym::index_t index;

        // b
        ::eigen_lcm::VectorXf residual;

        // J
        ::eigen_lcm::MatrixXf jacobian;

        // H, JtJ
        ::eigen_lcm::MatrixXf hessian;

        // Jtb
        ::eigen_lcm::VectorXf rhs;

    public:
        linearized_dense_factorf_t() = default;

        /**
         * Member constructor
         */
        inline linearized_dense_factorf_t(
            const ::sym::index_t& index_arg,
            const ::eigen_lcm::VectorXf& residual_arg,
            const ::eigen_lcm::MatrixXf& jacobian_arg,
            const ::eigen_lcm::MatrixXf& hessian_arg,
            const ::eigen_lcm::VectorXf& rhs_arg
        );

        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        using type_name_array_t = const char[27];

        inline static constexpr type_name_array_t* getTypeNameArrayPtr();

        /**
         * Returns "linearized_dense_factorf_t"
         */
        inline static constexpr const char* getTypeName();

        /**
         * Returns "sym"
         */
        inline static const char * getPackageName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
#if !defined(SKYDIO_DISABLE_LCM_NO_INLINE)
        __attribute__((noinline))
#endif
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);

        // Comparison operators.
        inline bool operator==(const linearized_dense_factorf_t& other) const;
        inline bool operator!=(const linearized_dense_factorf_t& other) const;
};

linearized_dense_factorf_t::linearized_dense_factorf_t(
    const ::sym::index_t& index_arg,
    const ::eigen_lcm::VectorXf& residual_arg,
    const ::eigen_lcm::MatrixXf& jacobian_arg,
    const ::eigen_lcm::MatrixXf& hessian_arg,
    const ::eigen_lcm::VectorXf& rhs_arg
) : index(index_arg),
    residual(residual_arg),
    jacobian(jacobian_arg),
    hessian(hessian_arg),
    rhs(rhs_arg) {}

int linearized_dense_factorf_t::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = (int64_t)getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int linearized_dense_factorf_t::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int linearized_dense_factorf_t::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t linearized_dense_factorf_t::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

constexpr linearized_dense_factorf_t::type_name_array_t* linearized_dense_factorf_t::getTypeNameArrayPtr() {
    return &"linearized_dense_factorf_t";
}

constexpr const char* linearized_dense_factorf_t::getTypeName()
{
    return *linearized_dense_factorf_t::getTypeNameArrayPtr();
}

const char * linearized_dense_factorf_t::getPackageName()
{
    return "sym";
}

int linearized_dense_factorf_t::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = this->index._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->residual._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->jacobian._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->hessian._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->rhs._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int linearized_dense_factorf_t::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = this->index._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->residual._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->jacobian._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->hessian._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->rhs._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int linearized_dense_factorf_t::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->index._getEncodedSizeNoHash();
    enc_size += this->residual._getEncodedSizeNoHash();
    enc_size += this->jacobian._getEncodedSizeNoHash();
    enc_size += this->hessian._getEncodedSizeNoHash();
    enc_size += this->rhs._getEncodedSizeNoHash();
    return enc_size;
}

#if !defined(SKYDIO_DISABLE_LCM_NO_INLINE)
__attribute__((noinline))
#endif
uint64_t linearized_dense_factorf_t::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == linearized_dense_factorf_t::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, linearized_dense_factorf_t::getHash };

    uint64_t hash = 0xde672a651e2fd789LL +
         ::sym::index_t::_computeHash(&cp) +
         ::eigen_lcm::VectorXf::_computeHash(&cp) +
         ::eigen_lcm::MatrixXf::_computeHash(&cp) +
         ::eigen_lcm::MatrixXf::_computeHash(&cp) +
         ::eigen_lcm::VectorXf::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

bool linearized_dense_factorf_t::operator==(const linearized_dense_factorf_t& other) const {
  return (
          (index==other.index) && 
          (residual==other.residual) && 
          (jacobian==other.jacobian) && 
          (hessian==other.hessian) && 
          (rhs==other.rhs));
}

bool linearized_dense_factorf_t::operator!=(const linearized_dense_factorf_t& other) const {
  return !(*this==other);
}

}

#endif