# This file automatically generated by skymarshal
# DO NOT MODIFY BY HAND
# fmt: off
# mypy: disallow-untyped-defs

import copy
import typing as T  # pylint: disable=unused-import

from io import BytesIO
import struct
from lcmtypes.sym._index_t import index_t

class valuesf_t(object):
    __slots__ = ["index", "data"]  # type: T.List[str]

    def __init__(
        self,
        index=None,  # type: index_t
        data=None,  # type: T.Sequence[float]
        _skip_initialize=False,  # type: bool
    ):
        # type: (...) -> None
        """ If _skip_initialize is True, all other constructor arguments are ignored """
        if _skip_initialize:
            return
        self.index = index_t._default() if index is None else index  # type: index_t
        self.data = [] if data is None else data  # type: T.Sequence[float]

    @staticmethod
    def _skytype_meta():
        # type: () -> T.Dict[str, str]
        return dict(
            type="struct",
            package="sym",
            name="valuesf_t",
        )

    @classmethod
    def _default(cls):
        # type: () -> valuesf_t
        return cls()

    def __repr__(self):
        # type: () -> str
        return "valuesf_t({})".format(
            ", ".join("{}={}".format(name, repr(getattr(self, name))) for name in self.__slots__))

    def __eq__(self, other):
        # type: (object) -> bool
        if not isinstance(other, valuesf_t):
            return NotImplemented
        return (
            (self.index==other.index) and
            (self.data==other.data)
        )
    # Disallow hashing for python struct lcmtypes.
    __hash__ = None  # type: ignore

    def encode(self):
        # type: () -> bytes
        buf = BytesIO()
        buf.write(valuesf_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        # type: (T.BinaryIO) -> None
        if hasattr(self.index, '_get_packed_fingerprint'):
            assert self.index._get_packed_fingerprint() == index_t._get_packed_fingerprint()
        else:
            assert self.index._get_hash_recursive([]) == index_t._get_hash_recursive([])
        self.index._encode_one(buf)
        v_num_data = len(self.data)
        buf.write(valuesf_t._CACHED_STRUCT_0.pack(v_num_data))
        buf.write(struct.pack('>%df' % v_num_data, *self.data[:v_num_data]))

    @staticmethod
    def decode(data):
        # type: (T.Union[bytes, T.BinaryIO]) -> valuesf_t
        # NOTE(eric): This function can technically accept either a BinaryIO or
        # anything that supports the C++ Buffer Protocol,
        # which is unspecifiable in type hints.

        if hasattr(data, "read"):
            # NOTE(eric): mypy isn't able to figure out the hasattr check
            buf = T.cast(T.BinaryIO, data)
        else:
            buf = BytesIO(T.cast(bytes, data))

        if buf.read(8) != valuesf_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return valuesf_t._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        # type: (T.BinaryIO) -> valuesf_t
        self = valuesf_t(_skip_initialize=True)
        self.index = index_t._decode_one(buf)
        v_num_data = valuesf_t._CACHED_STRUCT_0.unpack(buf.read(4))[0]
        self.data = struct.unpack('>%df' % v_num_data, buf.read(v_num_data * 4))
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        # type: (T.List[T.Type]) -> int
        if valuesf_t in parents: return 0
        newparents = parents + [valuesf_t]
        tmphash = (0x4c0cd46597b36c0f+ index_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash

    _packed_fingerprint = None  # type: T.Optional[bytes]

    @staticmethod
    def _get_packed_fingerprint():
        # type: () -> bytes
        if valuesf_t._packed_fingerprint is None:
            valuesf_t._packed_fingerprint = struct.pack(">Q", valuesf_t._get_hash_recursive([]))
        return valuesf_t._packed_fingerprint

    def deepcopy(self, **kwargs):
        # type: (**T.Any) -> valuesf_t
        """Ptree-style deep copy. Returns a copy w/ specified members replaced."""
        result = copy.deepcopy(self)
        for key in kwargs:
            if not hasattr(result, key):
                raise KeyError("Type valuesf_t does not have attribute: " + str(key))
            setattr(result, key, kwargs[key])
        return result

    _CACHED_STRUCT_0 = struct.Struct(">i")
